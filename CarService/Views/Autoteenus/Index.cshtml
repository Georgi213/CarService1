@model IEnumerable<Autoteenus>

@{
    ViewData["Title"] = "Index";
}

<h1>Autoteenus</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.autoteenus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hind)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aeg)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.autoteenus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hind)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aeg)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AutoteenusID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AutoteenusID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AutoteenusID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
